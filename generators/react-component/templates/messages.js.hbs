import { defineMessages } from 'react-intl';

export const scope = 'components.{{ properCase name }}';

export const {{ camelCase name }}Messages = defineMessages({
  hello: {
    id: `${scope}.hello`,
    defaultMessage: 'Hello {name}!!'
  }
});
