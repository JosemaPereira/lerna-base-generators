import { defineMessages } from 'react-intl';

const scope = 'components.Main';

export const mainMessages = defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Hello world!!'
  }
});
